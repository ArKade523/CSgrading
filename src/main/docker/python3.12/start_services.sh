#!/bin/bash

# Start Nginx in the background
nginx

# Start your Python web server here
# For example, if you're using Flask and your app is named app.py:
# python /submission/app.py

# Keep the container running indefinitely (if needed)
tail -f /dev/null
